const Animation = require("./classes/animation");

const animationInstance = new Animation();

animationInstance.animate(3, 'RR..LRL');

animationInstance.animate(2, '..R....');

animationInstance.animate(1, '...');

animationInstance.animate(1, 'LRRL.LR.LRR.R.LRRL.');

